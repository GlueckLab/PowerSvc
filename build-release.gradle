apply from: 'build-common.gradle'

version = '2.0.6.0'

dependencies {
    compile 'org.samplesizeshop:webservicecommon:1.1.4.0'
    compile('org.samplesizeshop:javastatistics:1.2.7.0') {
        exclude group: 'javax.jms'
        exclude group: 'com.sun.jdmk'
        exclude group: 'com.sun.jmx'
    }
}
